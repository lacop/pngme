// TODO: Remove eventually, but avoids warning spam for now.
#![allow(dead_code)]

mod args;
mod chunk;
mod chunk_type;
mod commands;
mod png;

use anyhow::Result;

fn main() -> Result<()> {
    todo!()
}
